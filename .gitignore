node_modules
**/node_modules
.next
**/.next
out
**/out
dist
**/dist
coverage
**/coverage
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.tsbuildinfo

# --- secrets -------------------------------------------------------------
# The previous version of this file had ".env.*" fused to UTF-16 bytes, so the
# pattern matched nothing and .env.local would have been committed.
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example

# --- model artefacts -----------------------------------------------------
incident_model_scripts/output/
*.pkl
*.h5
*.keras

# Large intermediate datasets (regenerate with 12_extract_incidents_only.py).
# Evaluation outputs under incident_model_scripts/outputs/models/ are kept:
# the JSON results, the report and the chart are the deliverable.
incident_model_scripts/outputs/dataset/
*.csv
!incident_model_scripts/outputs/models/*.csv
